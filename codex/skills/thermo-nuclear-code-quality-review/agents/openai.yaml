interface:
  display_name: "Thermo-Nuclear Code Quality Review"
  short_description: "Run a very strict maintainability audit"
  default_prompt: "Use $thermo-nuclear-code-quality-review to run an especially strict maintainability review of the recent changes."

policy:
  allow_implicit_invocation: false

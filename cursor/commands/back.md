Turn off away mode because I am back. Only I trigger this by typing /back; never invoke it on your own.

1. run `~/.claude/scripts/away-mode.sh disable`
2. report the script's output verbatim. away mode is off once it prints "Away mode OFF." if it printed an error instead, away mode is STILL ON.

Do not place any phone calls until I enable away mode again with /away.
